// GitHub OAuth credentials template.
// 1. cp GipetSecrets.swift.template GipetSecrets.swift
// 2. GipetSecrets.swift is gitignored — fill in your own credentials.
// Create your OAuth App at: https://github.com/settings/developers
//   Authorization callback URL: gipet://callback
enum GipetSecrets {
    static let githubClientID     = "YOUR_CLIENT_ID"
    static let githubClientSecret = "YOUR_CLIENT_SECRET"
}
